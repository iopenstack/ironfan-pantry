#!/bin/bash

exec 2>&1
exec chpst -u <%= @options[:user] %>:<%= @options[:group] %> /usr/sbin/gmond --conf=<%= @options[:dirs][:conf] %>/gmond.<%= @options[:cluster][:name] %>.conf --debug=3 --foreground
 
